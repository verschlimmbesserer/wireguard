---

wireguard_key_filename: "{{ ansible_hostname }}"
wireguard_ipv4_forwarding: true
wireguard_ipv6_forwarding: true
wireguard_generate_keys: true
wireguard_generate_presharedkey: false
wireguard_state: present
wireguard_config: {}
